<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>TechHub</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,300;0,400;0,700;1,800&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <link rel="stylesheet" href="/stylesheets/header.css" />
    <link rel="stylesheet" href="/stylesheets/profile.css" />
    <link rel="stylesheet" href="/stylesheets/footer.css" />
  </head>
  <body>
    <%- include('header') %>
    <main>
      <section class="profile">
        <div class="heading">
          <h1>Hello, <%= currentUser.firstName %></h1>
          <div class="main-divider"></div>
        </div>
        <% if (currentUser.isAdmin) { %>
        <div class="already-admin-heading">
          <h2>You're already an admin</h2>
          <img src="/images/icons/check.svg" alt="Check" />
        </div>
        <% } else { %>
        <form method="post">
          <label for="admin-password">Admin Password</label>
          <div>
            <input
              name="adminPassword"
              id="admin-password"
              type="password"
              min="1"
              max="16"
              required
            />
            <button class="get-access-button">Get access</button>
          </div>
        </form>
        <% } %>
        <a href="/log-out" class="log-out-button">Log Out</a>
      </section>
    </main>
    <%- include('footer') %>
  </body>
</html>
